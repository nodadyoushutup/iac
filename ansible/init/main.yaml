---
- name: List directory contents
  hosts: localhost
  become: false
  gather_facts: false

  tasks:
    - name: Show current directory contents
      command: ls -la .

    - name: Show parent directory contents
      command: ls -la ..
