- name: Free strategy with ping check
  hosts: all
  strategy: free
  gather_facts: false

  tasks:
    - name: Ping each host
      ping:
      register: ping_result
      ignore_unreachable: true

    - name: Run only if host is reachable
      debug:
        msg: "Host is reachable"
      when: ping_result.ping is defined and ping_result.ping == 'pong'
