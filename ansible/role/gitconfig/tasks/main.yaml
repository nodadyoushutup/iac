---
- name: Copy .gitconfig to the ansible user's home
  template:
    src: .gitconfig.j2
    dest: /home/{{ ansible_user }}/.gitconfig
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0644'
