---
- name: Debug
  debug:
    var: config[key]

- name: Add host from config
  add_host:
    name: "{{ key }}"
    ansible_host: "{{ config[key].machine.host }}"
    ansible_port: "{{ config[key].machine.port }}"
    groups: "{{ key }}"
  delegate_to: localhost
  run_once: true
