LABEL=cloudimg-rootfs   /        ext4   discard,errors=remount-ro       0 1  
LABEL=UEFI      /boot/efi       vfat    umask=0077      0 1

{% for mount in config.fstab %}
{{ mount.host }}:{{ mount.src }} {{ mount.dest }} {{ mount.opt }}
{% endfor %}