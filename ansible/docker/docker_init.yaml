---
- name: Init Docker
  hosts: docker
  gather_facts: no
  become: true
  check_mode: no
  roles:
    - ./roles/vm_init
    - ./roles/docker_install
    - ./roles/docker_compose_down
    - ./roles/docker_compose_up