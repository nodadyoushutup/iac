pipeline {
  agent any

  options {
    ansiColor('xterm')
  }

  stages {
    stage('Verify Terraform CLI') {
      steps {
        dir('terraform') {
          sh 'terraform version'
        }
      }
    }
  }
}
