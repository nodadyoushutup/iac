pipeline {
  agent any

  options {
    ansiColor('xterm')
  }

  stages {
    stage('Verify Terraform CLI') {
      steps {
        sh 'command -v terraform >/dev/null 2>&1'
        sh 'terraform version'
      }
    }
  }
}
